@page
@model IEnumerable<Pessoa>
@{
    ViewData["Title"] = "Pessoas";
}

<h1>Pessoas</h1>

<ul>
    @foreach (var pessoa in Model)
    {
        <li>@pessoa.Nome - @pessoa.Idade anos - CPF: @pessoa.CPF</li>
    }
</ul>